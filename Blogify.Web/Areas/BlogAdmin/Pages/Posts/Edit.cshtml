@page
@model Blogify.Web.Areas.BlogAdmin.Pages.Posts.EditModel
@{
    ViewData["Title"] = Localizer["Page.Title.BlogAdmin.EditPost"].Value + " — " + Model.PostTitle;
    ViewData["EditorMode"] = true;
    string? blogSlug = ViewContext.RouteData.Values["blogSlug"]?.ToString();
}

@if (Model.SavedToastType is not null)
{
    <div class="toast-container position-fixed top-0 end-0 p-3" style="z-index:1090" aria-live="polite">
        <div id="saveToast" class="toast align-items-center text-bg-success border-0"
             role="status" aria-atomic="true"
             data-bs-autohide="true" data-bs-delay="4000">
            <div class="d-flex">
                <div class="toast-body">
                    @if (Model.SavedToastType == "published")
                    {
                        @Localizer["Message.PostPublished"]
                    }
                    else if (Model.SavedToastType == "unpublished")
                    {
                        @Localizer["Message.PostUnpublished"]
                    }
                    else
                    {
                        @Localizer["Message.PostSaved"]
                    }
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto"
                        data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
        </div>
    </div>
}

<form method="post" id="editPostForm" class="pe-studio" novalidate
      data-post-editor-page data-editor-mode="edit" data-is-published="@Model.Input.IsPublished.ToString().ToLowerInvariant()"
      data-unsaved-label="@Localizer["Editor.Unsaved"].Value"
      data-remove-label="@Localizer["Action.Remove"].Value"
      data-saved-label="@Localizer["Editor.Saved"].Value"
      data-show-saved="@((Model.SavedToastType is not null).ToString().ToLowerInvariant())"
      data-secondary-action-label="@(Model.Input.IsPublished ? Localizer["Action.Unpublish"].Value : Localizer["Action.SaveDraft"].Value)"
      data-primary-action-label="@(Model.Input.IsPublished ? Localizer["Action.Update"].Value : Localizer["Action.Publish"].Value)">

    <div class="pe-header">
        <div class="d-flex align-items-center gap-2 flex-wrap">
            <a asp-area="BlogAdmin" asp-page="/Posts/Index"
               asp-route-blogSlug="@blogSlug"
               class="btn btn-sm btn-outline-secondary d-inline-flex align-items-center gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"
                     viewBox="0 0 16 16" aria-hidden="true">
                    <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8" />
                </svg>
                @Localizer["Nav.Posts"]
            </a>
            @if (Model.Input.IsPublished)
            {
                <span class="badge bg-success">@Localizer["Post.Status.Published"]</span>
            }
            else
            {
                <span class="badge bg-secondary">@Localizer["Post.Status.Draft"]</span>
            }
            <span class="pe-save-status ms-1" id="peSaveStatus" aria-live="polite"></span>
            <button type="button" class="btn btn-sm btn-outline-secondary ms-auto pe-settings-open"
                    aria-controls="postSettingsDrawer" aria-expanded="false">
                @Localizer["Editor.Settings"]
            </button>
            <div class="d-none d-lg-flex gap-2">
                @if (Model.Input.IsPublished)
                {
                    <button type="submit" name="Input.IsPublished" value="false"
                            class="btn btn-sm btn-outline-secondary">
                        @Localizer["Action.Unpublish"]
                    </button>
                    <button type="submit" name="Input.IsPublished" value="true"
                            class="btn btn-sm btn-primary">
                        @Localizer["Action.Update"]
                    </button>
                }
                else
                {
                    <button type="submit" name="Input.IsPublished" value="false"
                            class="btn btn-sm btn-outline-secondary">
                        @Localizer["Action.SaveDraft"]
                    </button>
                    <button type="submit" name="Input.IsPublished" value="true"
                            class="btn btn-sm btn-primary">
                        @Localizer["Action.Publish"]
                    </button>
                }
            </div>
        </div>
    </div>

    <div asp-validation-summary="ModelOnly" class="alert alert-danger mt-2" role="alert"></div>

    <div class="pe-content-area">

        <div class="pe-writing-column">

            <label for="postTitle" class="visually-hidden">@Localizer["Label.Title"]</label>
            <textarea id="postTitle" name="Input.Title" class="pe-title-input"
                      rows="1" placeholder="@Localizer["Label.Title"].Value…"
                      autocomplete="off" maxlength="500">@Model.Input.Title</textarea>
            <span asp-validation-for="Input.Title" class="text-danger small d-block"></span>

            <div class="pe-slug-wrap my-2" id="slugDisplayWrap">
                <span class="pe-slug-display">/&nbsp;<span id="slugDisplay">@Model.Input.Slug</span></span>
                <button type="button" class="pe-slug-edit-btn" id="slugEditBtn"
                        aria-label="@Localizer["Action.EditUrl"].Value">&#9998;</button>
            </div>

            <label id="postEditorLabel" class="visually-hidden">@Localizer["Label.Content"]</label>
            <div id="postEditorWrapper" class="post-editor-wrapper"
                 role="group" aria-labelledby="postEditorLabel"></div>
            <textarea asp-for="Input.Content" id="postEditorHidden"
                      class="visually-hidden" tabindex="-1" aria-hidden="true"></textarea>
            <span asp-validation-for="Input.Content" class="text-danger small"></span>

            <div class="pe-editor-footer">
                <span id="wordCountDisplay" aria-live="polite"
                      data-count-format="@Localizer["Editor.WordCountFormat"].Value">@Localizer["Editor.WordCountFormat", 0, 0]</span>
            </div>
        </div>

        <div class="pe-drawer-backdrop" data-settings-close tabindex="-1"></div>
        <aside class="pe-settings-drawer" id="postSettingsDrawer" role="dialog" aria-modal="true"
               aria-labelledby="postSettingsTitle" aria-hidden="true" inert>
            <div class="pe-drawer-header">
                <h2 id="postSettingsTitle">@Localizer["Editor.Settings"]</h2>
                <button type="button" class="btn-close" data-settings-close aria-label="@Localizer["Action.Close"].Value"></button>
            </div>
            <div class="pe-settings-sidebar">
                <h3 class="pe-settings-group-title">@Localizer["Editor.PostDetails"]</h3>

                <div class="pe-status-panel">
                    <div class="d-flex align-items-center gap-2 mb-1">
                        @if (Model.Input.IsPublished)
                        {
                            <span class="badge bg-success">@Localizer["Post.Status.Published"]</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">@Localizer["Post.Status.Draft"]</span>
                        }
                    </div>
                    <div class="text-muted small">
                        @Localizer["Label.Author"]: @Model.AuthorName
                    </div>
                    <div class="text-muted small">
                        @Localizer["Label.PostStatus"]: @Model.PostCreatedAtFormatted
                    </div>
                </div>

                <div class="pe-settings-section pe-url-setting" id="slugEditWrap">
                    <label asp-for="Input.Slug" class="form-label small fw-semibold">@Localizer["Label.Slug"]</label>
                    @if (Model.Input.IsPublished)
                    {
                        <div class="pe-slug-warn mb-2">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"
                                 viewBox="0 0 16 16" aria-hidden="true">
                                <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5m.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2" />
                            </svg>
                            @Localizer["Message.SlugChangeWarning"]
                        </div>
                    }
                    <div class="input-group">
                        <span class="input-group-text text-muted">/</span>
                        <input asp-for="Input.Slug" class="form-control" id="postSlug"
                               placeholder="my-post-slug" autocomplete="off" />
                    </div>
                    <div class="d-flex align-items-center justify-content-between mt-1">
                        <div class="form-text">@Localizer["Label.Slug.Help.Edit"]</div>
                        <button type="button" class="btn btn-link btn-sm p-0 text-muted" id="slugDoneBtn">
                            @Localizer["Action.Close"]
                        </button>
                    </div>
                    <span asp-validation-for="Input.Slug" class="text-danger small"></span>
                </div>

                <div class="pe-settings-section">
                    <button type="button" class="pe-settings-toggle"
                            data-bs-toggle="collapse" data-bs-target="#featuredImageCollapse"
                            aria-expanded="true" aria-controls="featuredImageCollapse">
                        @Localizer["Label.FeaturedImage"]
                        <span class="pe-toggle-icon" aria-hidden="true">&#9660;</span>
                    </button>
                    <div id="featuredImageCollapse" class="collapse show pe-settings-body">
                        <partial name="_MediaPickerField"
                                 model="@(new MediaPickerFieldVm("coverImageId", "Input.CoverImageId", Model.Input.CoverImageId, Model.CoverImagePreviewUrl, "mediaPickerModal"))" />
                    </div>
                </div>

                <h3 class="pe-settings-group-title">@Localizer["Editor.Organization"]</h3>
                <div class="pe-settings-section">
                    <button type="button" class="pe-settings-toggle"
                            data-bs-toggle="collapse" data-bs-target="#categoriesCollapse"
                            aria-expanded="true" aria-controls="categoriesCollapse">
                        @Localizer["Nav.Categories"]
                        <span class="badge bg-secondary ms-1" id="categoriesBadge"
                              aria-live="polite"
                              style="@(Model.AvailableCategories.Count(c => c.IsSelected) == 0 ? "display:none" : "")">
                            @Model.AvailableCategories.Count(c => c.IsSelected)
                        </span>
                        <span class="pe-toggle-icon" aria-hidden="true">&#9660;</span>
                    </button>
                    <div id="categoriesCollapse" class="collapse show pe-settings-body">
                        @if (Model.AvailableCategories.Count == 0)
                        {
                            <p class="text-muted small mb-0">
                                @Localizer["Label.Categories.None"]
                                <a asp-area="BlogAdmin" asp-page="/Categories/Create" asp-route-blogSlug="@blogSlug">@Localizer["Label.Categories.Create"]</a>.
                            </p>
                        }
                        else
                        {
                            <div aria-hidden="true" style="display:none" id="categoryCheckboxes">
                                @foreach (CategorySelectItem category in Model.AvailableCategories)
                                {
                                    <div>
                                        <input type="checkbox" name="SelectedCategoryIds"
                                               value="@category.Id" id="cat_@category.Id"
                                               @(category.IsSelected ? "checked" : "") />
                                        <label for="cat_@category.Id">@category.Name</label>
                                    </div>
                                }
                            </div>
                            <div class="pe-category-pills mb-2" id="categoryPills"
                                 aria-live="polite"
                                 aria-label="@Localizer["Nav.Categories"].Value"></div>
                            <div class="pe-category-search-wrap" data-taxonomy-picker
                                 data-checkboxes="categoryCheckboxes"
                                 data-pills="categoryPills"
                                 data-search="categorySearch"
                                 data-dropdown="categoryDropdown"
                                 data-badge="categoriesBadge">
                                <input type="text" class="form-control form-control-sm"
                                       id="categorySearch" autocomplete="off"
                                       placeholder="@Localizer["Label.Categories.Search"].Value"
                                       aria-label="@Localizer["Label.Categories.Search"].Value" />
                                <div id="categoryDropdown" class="pe-category-dropdown" role="listbox"></div>
                            </div>
                        }
                    </div>
                </div>

                <div class="pe-settings-section">
                    <button type="button" class="pe-settings-toggle"
                            data-bs-toggle="collapse" data-bs-target="#tagsCollapse"
                            aria-expanded="true" aria-controls="tagsCollapse">
                        @Localizer["Nav.Tags"]
                        <span class="badge bg-secondary ms-1" id="tagsBadge"
                              aria-live="polite"
                              style="@(Model.AvailableTags.Count(t => t.IsSelected) == 0 ? "display:none" : "")">
                            @Model.AvailableTags.Count(t => t.IsSelected)
                        </span>
                        <span class="pe-toggle-icon" aria-hidden="true">&#9660;</span>
                    </button>
                    <div id="tagsCollapse" class="collapse show pe-settings-body">
                        @if (Model.AvailableTags.Count == 0)
                        {
                            <p class="text-muted small mb-0">
                                @Localizer["Label.Tags.None"]
                                <a asp-area="BlogAdmin" asp-page="/Tags/Create" asp-route-blogSlug="@blogSlug">@Localizer["Label.Tags.Create"]</a>.
                            </p>
                        }
                        else
                        {
                            <div aria-hidden="true" style="display:none" id="tagCheckboxes">
                                @foreach (TagSelectItem tag in Model.AvailableTags)
                                {
                                    <div>
                                        <input type="checkbox" name="SelectedTagIds"
                                               value="@tag.Id" id="tag_@tag.Id"
                                               @(tag.IsSelected ? "checked" : "") />
                                        <label for="tag_@tag.Id">@tag.Name</label>
                                    </div>
                                }
                            </div>
                            <div class="pe-category-pills mb-2" id="tagPills"
                                 aria-live="polite"
                                 aria-label="@Localizer["Nav.Tags"].Value"></div>
                            <div class="pe-category-search-wrap" data-taxonomy-picker
                                 data-checkboxes="tagCheckboxes"
                                 data-pills="tagPills"
                                 data-search="tagSearch"
                                 data-dropdown="tagDropdown"
                                 data-badge="tagsBadge">
                                <input type="text" class="form-control form-control-sm"
                                       id="tagSearch" autocomplete="off"
                                       placeholder="@Localizer["Label.Tags.Search"].Value"
                                       aria-label="@Localizer["Label.Tags.Search"].Value" />
                                <div id="tagDropdown" class="pe-category-dropdown" role="listbox"></div>
                            </div>
                        }
                    </div>
                </div>

                <div class="pe-settings-section">
                    <button type="button" class="pe-settings-toggle"
                            data-bs-toggle="collapse" data-bs-target="#excerptCollapse"
                            aria-expanded="true" aria-controls="excerptCollapse">
                        @Localizer["Label.Excerpt"]
                        <span class="pe-toggle-icon" aria-hidden="true">&#9660;</span>
                    </button>
                    <div id="excerptCollapse" class="collapse show pe-settings-body">
                        <label asp-for="Input.Excerpt" class="visually-hidden">@Localizer["Label.Excerpt"]</label>
                        <textarea asp-for="Input.Excerpt" class="form-control form-control-sm" rows="3"
                                  id="excerptInput"
                                  placeholder="@Localizer["Label.Excerpt.Placeholder"].Value"></textarea>
                        <div class="d-flex justify-content-between align-items-baseline mt-1">
                            <div class="form-text">@Localizer["Label.Excerpt.Help"]</div>
                            <span class="pe-char-count" id="excerptCount" aria-live="polite">0/500</span>
                        </div>
                        <span asp-validation-for="Input.Excerpt" class="text-danger small"></span>
                    </div>
                </div>

                <h3 class="pe-settings-group-title">@Localizer["Editor.SearchSharing"]</h3>
                <div class="pe-settings-section">
                    <button type="button" class="pe-settings-toggle"
                            data-bs-toggle="collapse" data-bs-target="#seoCollapse"
                            aria-expanded="false" aria-controls="seoCollapse">
                        @Localizer["Label.Seo"]
                        <span class="pe-toggle-icon" aria-hidden="true">&#9660;</span>
                    </button>
                    <div id="seoCollapse" class="collapse pe-settings-body">
                        <div class="mb-3">
                            <div class="d-flex justify-content-between align-items-baseline mb-1">
                                <label asp-for="Input.MetaTitle"
                                       class="form-label small fw-semibold mb-0">@Localizer["Label.MetaTitle"]</label>
                                <span class="pe-char-count" id="metaTitleCount" aria-live="polite">0/60</span>
                            </div>
                            <input asp-for="Input.MetaTitle" class="form-control form-control-sm"
                                   id="metaTitleInput"
                                   placeholder="@Localizer["Label.MetaTitle.Placeholder"].Value"
                                   autocomplete="off" />
                            <span asp-validation-for="Input.MetaTitle" class="text-danger small"></span>
                        </div>
                        <div class="mb-2">
                            <div class="d-flex justify-content-between align-items-baseline mb-1">
                                <label asp-for="Input.MetaDescription"
                                       class="form-label small fw-semibold mb-0">@Localizer["Label.MetaDescription"]</label>
                                <span class="pe-char-count" id="metaDescCount" aria-live="polite">0/160</span>
                            </div>
                            <textarea asp-for="Input.MetaDescription" class="form-control form-control-sm"
                                      id="metaDescInput" rows="3"
                                      placeholder="@Localizer["Label.MetaDescription.Placeholder"].Value"></textarea>
                            <span asp-validation-for="Input.MetaDescription" class="text-danger small"></span>
                        </div>
                        <div class="pe-serp-preview" aria-label="Search result preview">
                            <div class="pe-serp-url">yoursite.com/&thinsp;<span id="serpSlug">@Model.Input.Slug</span></div>
                            <div class="pe-serp-title" id="serpTitle">@(Model.Input.MetaTitle ?? Model.Input.Title)</div>
                            <div class="pe-serp-desc" id="serpDesc">@(Model.Input.MetaDescription ?? Localizer["Label.MetaDescription.Placeholder"].Value)</div>
                        </div>
                    </div>
                </div>

            </div>
        </aside>
    </div>

    <div class="pe-mobile-actions d-lg-none" aria-label="Post actions">
        @if (Model.Input.IsPublished)
        {
            <button type="submit" name="Input.IsPublished" value="false"
                    class="btn btn-outline-secondary flex-fill">
                @Localizer["Action.Unpublish"]
            </button>
            <button type="submit" name="Input.IsPublished" value="true"
                    class="btn btn-primary flex-fill">
                @Localizer["Action.Update"]
            </button>
        }
        else
        {
            <button type="submit" name="Input.IsPublished" value="false"
                    class="btn btn-outline-secondary flex-fill">
                @Localizer["Action.SaveDraft"]
            </button>
            <button type="submit" name="Input.IsPublished" value="true"
                    class="btn btn-primary flex-fill">
                @Localizer["Action.Publish"]
            </button>
        }
    </div>

    <input type="hidden" id="editorImageInsert" />
    <partial name="_EditorImagePickerModal" model='"editorImageInsert"' />
    <partial name="_MediaPickerDialog"
             model="@(new MediaPickerFieldVm("coverImageId", "Input.CoverImageId", Model.Input.CoverImageId, Model.CoverImagePreviewUrl, "mediaPickerModal", RecommendedDimensionsHint: Localizer["Label.FeaturedImage.Recommended"].Value))" />
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <link rel="stylesheet" href="~/css/post-editor.css" asp-append-version="true" />
    <script type="module">
        import { initPostEditor } from '/js/post-editor.js';
        await initPostEditor('postEditorWrapper', 'postEditorHidden', 'editPostForm');
    </script>
    <script src="~/js/post-editor-page.js" asp-append-version="true"></script>
}
