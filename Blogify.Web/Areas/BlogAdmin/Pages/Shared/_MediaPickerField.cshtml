@model Blogify.Web.Areas.BlogAdmin.Pages.Media.MediaPickerFieldVm

<input type="hidden" id="@Model.InputId" name="@Model.InputName"
       value="@(Model.CurrentMediaId.HasValue ? Model.CurrentMediaId.Value.ToString() : string.Empty)" />

<div id="@(Model.InputId)-preview" class="mb-2 @(!Model.CurrentMediaId.HasValue ? "d-none" : "")">
    <img src="@Model.CurrentThumbnailUrl" alt="Cover image preview"
         class="img-fluid rounded border" style="max-height: 160px; object-fit: cover;" />
</div>

<div class="d-flex align-items-center gap-2 flex-wrap">
    <button type="button" class="btn btn-outline-secondary btn-sm"
            data-bs-toggle="modal"
            data-bs-target="#@Model.ModalId">
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor"
             class="me-1" viewBox="0 0 16 16" aria-hidden="true">
            <path d="M6.502 7a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3"/>
            <path d="M14 14a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V2a2 2 0 0 1 2-2h5.5L14 4.5zM4 1a1 1 0 0 0-1 1v10l2.224-2.224a.5.5 0 0 1 .61-.075L8 11l2.157-3.02a.5.5 0 0 1 .76-.063L13 10V4.5h-2A1.5 1.5 0 0 1 9.5 3V1z"/>
        </svg>
        Choose from Library
    </button>
    <button type="button" class="btn btn-outline-danger btn-sm @(!Model.CurrentMediaId.HasValue ? "d-none" : "")"
            id="@(Model.InputId)-clear"
            data-picker-clear
            data-target-input-id="@Model.InputId">
        Remove
    </button>
</div>
