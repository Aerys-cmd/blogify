@page
@model Blogify.Web.Areas.BlogAdmin.Pages.Settings.IndexModel
@{
    ViewData["Title"] = @Localizer["Page.Title.BlogAdmin.Settings"].Value;
}

<header class="b-page-header"><div><p class="b-page-context">Manage</p><h1>@Localizer["Page.Title.BlogAdmin.Settings"]</h1><p>Shape how your blog is described and discovered.</p></div></header>
<nav class="b-settings-nav" aria-label="Settings sections">
    <span>General</span>
    <a asp-area="BlogAdmin" asp-page="/Themes/Index" asp-route-blogSlug="@ViewContext.RouteData.Values["blogSlug"]">Appearance</a>
    <strong>SEO</strong>
    <a asp-area="BlogAdmin" asp-page="/Members/Index" asp-route-blogSlug="@ViewContext.RouteData.Values["blogSlug"]">Members</a>
</nav>

@if (TempData["SuccessMessage"] is string success)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @success
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="b-settings-layout">
        <section class="b-form-section">
            <div><h2>@Localizer["Label.Seo"]</h2><p>Set the summary people see in search results and link previews.</p></div>
            <div>
                <form method="post" novalidate>
                    @if (!Model.ModelState.IsValid)
                    {
                        <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                    }

                    <div class="mb-4">
                        <label asp-for="Input.MetaDescription" class="form-label fw-semibold">@Localizer["Label.MetaDescription"]</label>
                        <textarea asp-for="Input.MetaDescription" class="form-control" rows="3"
                                  placeholder="@Localizer["Label.MetaDescription.Blog.Placeholder"].Value"></textarea>
                        <div class="form-text">@Localizer["Label.MetaDescription.Help.Blog"]</div>
                        <span asp-validation-for="Input.MetaDescription" class="text-danger small"></span>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">@Localizer["Action.Save"]</button>
                    </div>
                </form>
            </div>
        </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
