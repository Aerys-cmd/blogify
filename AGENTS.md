# Blogify Agent Guide

Codex should read `.github/copilot-instructions.md` first, then apply the file-scoped instructions under `.github/instructions/` when editing matching files.

For architecture-sensitive changes, read `.github/architecture/system-overview.md`.

After the end of every session, update the relevant docs if necessary.
